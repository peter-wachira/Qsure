<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.wazinsure.qsure">

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/astronutavatar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.CustomerUpdateActivity"></activity>
        <activity android:name=".UI.CustomerUpdateFragment" />
        <activity android:name=".UI.CustomerDetailActivity" />
        <activity android:name=".UI.DisplayCustomersActivity" />
        <activity android:name=".UI.RegistrationActivity" />
        <activity android:name=".UI.AddCustomerActivity" />
        <activity android:name=".UI.MainActivity" />
        <activity
            android:name=".UI.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>